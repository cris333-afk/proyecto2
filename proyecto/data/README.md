# data/

- Acá van los archivos **.csv de entrada** del proyecto (se versionan: dispositivos de la red, rutas del árbol de directorios, conexiones del grafo).
- El log `network_audit_log.txt` y los `*.log` se generan en ejecución y **NO** se versionan (ver reglas en el `.gitignore`).
